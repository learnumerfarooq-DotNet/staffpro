name: CI

on:
push:
branches:
- main
pull_request:

permissions:
actions: read
contents: read

jobs:
<<<<<<< nx-cloud-setup
main:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout repository
    uses: actions/checkout@v4
    with:
      filter: tree:0
      fetch-depth: 0

  # Start Nx Cloud distributed execution early
  - name: Start Nx Cloud CI run
    run: npx nx start-ci-run --distribute-on="3 linux-medium-js" --stop-agents-after="e2e"

  # Setup Node.js with dependency caching
  - name: Setup Node.js
    uses: actions/setup-node@v4
    with:
      node-version: 20
      cache: npm

  # Install dependencies
  - name: Install dependencies
    run: npm ci

  # Check formatting
  - name: Check formatting
    run: npx nx record -- npx nx format:check --base="remotes/origin/main"

  # Run lint, tests, build, typecheck and e2e
  - name: Run Nx tasks
    run: npx nx run-many -t lint test build typecheck e2e

  # Apply Nx Cloud self-healing suggestions
  - name: Apply Nx Cloud fixes
    if: always()
    run: npx nx fix-ci
```
=======
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          filter: tree:0
          fetch-depth: 0

      # Step 1: Start Nx Cloud CI Run with the Access Token
      - name: Start Nx Cloud CI Run
        run: npx nx start-ci-run --distribute-on="3 linux-medium-js" --stop-agents-after="e2e-ci"
        env:
          NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check Format
        run: npx nx record -- npx nx format:check --base="remotes/origin/main"
        env:
          NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}

      - name: Run Tasks
        run: npx nx run-many -t lint test build typecheck e2e-ci
        env:
          NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}

      - name: Fix CI (if needed)
        run: npx nx fix-ci
        if: always()
        env:
          NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}
>>>>>>> main
